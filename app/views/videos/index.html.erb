<% provide(:title, 'Home') %>
<div class="center jumbotron">
  <h1>Gems Sound</h1>

  <h2>
    Youtube Iframe
    ここに動画表示（中央）

    <% @videos.shuffle.first(1).each do |video| %>
    <%=link_to "#{video.artist_name}","videos/#{video.id}" %>
    <% iframe = content_tag(
      :iframe,
      '', # empty body
      width: 560,
      height: 315,
      src: "https://www.youtube.com/embed/#{video.youtube_id}?rel=0",
      # src: "https://i.ytimg.com/vi/#{video.youtube_id}/1.jpg",
      frameborder: 0,
      allowfullscreen: true
    ) %>
    <%= content_tag(:div, iframe, class: 'youtube-container') %>
    <% end %>
  </div>




<div class="category">
  <div class="pickup">
    <h3 class="container bg-primary" >ピックアップ動画</h3><!--bootstrapのbg-xxxで背景色が変更-->
    <div class="video-lists">
    サムネイルを作成して、サムネイルをクリックするとshowページへ飛ぶ様にしたい(実装予定)</br>
    動画、サムネイルで動作が重くなるので、その対策。(実装予定)</br>
    </div>
  </div>

  <div class="cate1">
    <h3 class="container bg-success">ロック</h3>
      <div class="video-lists">
        <%= render 'iframe/iframe_image' %>
      </div>
  </div>

  <div class="cate2">
    <h3 class="container bg-danger">ギターロック</h3>
    <div class="video-lists">

    </div>
  </div>

  <div class="cate3">
    <h3 class="container bg-primary">ポストロック</h3>
    <div class="video-lists">
      <%= render 'iframe/iframe_image' %>
    </div>
  </div>

  <div class="cate4">
    <h3 class="container bg-danger">プログレ</h3>
    <div class="video-lists">

    </div>
  </div>

  <div class="cate5">
    <h3 class="container bg-primary">エモーショナル</h3>
    <div class="video-lists">
      <%= render 'iframe/iframe_image' %>
    </div>
  </div>
</div>
